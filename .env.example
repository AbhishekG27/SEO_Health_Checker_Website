# Google PageSpeed Insights API key (required)
# Enable "PageSpeed Insights API" in Google Cloud Console
GOOGLE_PAGESPEED_API_KEY=your_google_api_key_here

# Serper API key (optional — for SERP / ranking context in the report)
# Get key at https://serper.dev
SERPER_API_KEY=

# Gemini API key (optional — for AI gap analysis after the report)
# Get key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Scheduled daily audit — URL to audit (default: https://tekspotedu.com/)
# SCHEDULED_AUDIT_URL=https://tekspotedu.com/

# Google Docs (OAuth client secret — each report creates new Docs)
# GOOGLE_CREDENTIALS_FILE=credentials.json

# Google Drive folder name where Docs are stored (creates folder automatically on first run)
# Default: SEO Health Checker
# GOOGLE_DRIVE_FOLDER_NAME=SEO Health Checker
